# 项目交接 · 亚太统计分析系统（2026-09-23）

> **交接对象**：接手本仓库的开发 / 维护者 / AI Agent
> **交接时间**：2026-09-23
> **交接基线**：以 `git log --oneline -1` 为准（本文件不写死提交哈希）
> **本文件性质**：**自包含**——只需读这一份，即可接手本项目。
> 备份基线文件：`HANDOFF.md`（2026-09-17 旧版交接，含更细的历史约定，作为附录参考）。

---

## 0. 一分钟速览

| 项 | 内容 |
|---|---|
| 对外名称 | Asia-Pacific Statistical Analysis System（亚太统计分析系统） |
| 一句话定位 | 用**公开真实数据**做的亚太宏观经济统计 + **AI 解读**看板 |
| 数据来源 | 世界银行 Open Data · 中国国家统计局 · 海关总署（全部公开、无需鉴权） |
| 技术栈 | Python 3.11/3.12 + Flask + SQLAlchemy Core + SQLite；前端原生 HTML/CSS/JS，**零构建** |
| 仓库 | Gitee `cpufreestyle/statistical-analysis-system`（origin）· GitHub 同名（github） |
| 主分支 | `master`（生产分支）；已发版至 `v1.5.2` |
| 线上 | Vercel 项目 `qu-stat-system` → https://qu-stat-system.vercel.app |
| 测试 / CI | `tests/` **239 项**（pytest）+ `scripts/check_i18n.py` + `basedpyright` 类型门禁 + markdownlint（`ci.yml`） |
| 许可证 | 代码 MIT（`LICENSE`）；`data/` 沿用来源方条款（World Bank CC BY 4.0） |
| **唯一护城河** | **AI 解读 + 每行数据可溯源到 `note` + 明确禁止编造数字** |

---

## 1. 本次交接的核心：2026-09-23 变更

> 本轮由 **InfiniSynapse Agent** 完成，共 5 个批次（A–E），**全部已完成并验证**。
> 详细记录（人读）：`INFINISYNAPSE_INTEGRATION.md`；机读：`AGENT_CHANGES.json`。

| 批次 | 主题 | 关键落点 |
|---|---|---|
| **A** | AI 基于**真实数据**解读（facts 注入） | `src/analyzer.py` 新增 `files`/`images` 入参 + `build_facts_files()`；`cli.py`/`web.py`/`report.py` 接线 |
| **B** | **SQL 计算引擎**（1 条 SQL 绑定全部变量） | **新增** `src/stats/sql_engine.py` |
| **C** | 按 **agent_infini Skill 规范**强化集成 | **新增** `src/infini_skill.py`；`analyzer.py`/`web.py` 接线 |
| **D** | **数据库优化**（索引 / 写入 / 连接） | `src/db.py`：复合唯一索引 + 维度索引 + `ON CONFLICT` + PRAGMA |
| **E** | **性能与缓存优化**（P0+P1） | `init_db` 只跑一次、连接池、消除重复点查、gzip/ETag、AI 超时对齐 |
| **F** | **前端载荷优化**（字典拆分 / 样式外置 / 按资产版本号） | `public/` 三文件拆分 + `public/landing.css` + `src/web.py` 按资产算版本号；落地页首访 gzip **−16.3%**，看板 **+1.05%**（诚实记录的回退） |

**量化收益**：精确查找 **12.1×**、批量 upsert **14.4×**、`init_db` 热请求 3.5ms→0.001ms、大 JSON 压缩 **-83%**、重复请求 **304**。

**兼容性**：对外签名与返回结构**不变**；不设任何环境变量即走原逻辑（**零行为变更**）。

---

## 2. 上手：跑起来（3 条命令）

```bash
# 1) 依赖（Windows 用仓库内 venv）
.venv/Scripts/python.exe -m pip install -r requirements.txt

# 2) 起服务（默认绑定 0.0.0.0:5000）
.venv/Scripts/python.exe -m src.cli web --port 5000

# 3) 打开 http://127.0.0.1:5000/ （落地页）与 /app （看板）
```

**冷启动会自动播种**：库为空时载入 `data/ap_macro.csv` + `data/nbs_cn.csv` 与知识库种子，**幂等、不覆盖**已有数据。

自检：

```bash
python -m src.cli ask "2024 GDP" --lang en          # 本地自然语言查询
python -m src.cli report --year 2024 --dimension 中国 --lang zh
python -m src.cli db info                            # 库状态
python -m pytest -q                                  # 239 项测试
python scripts/check_i18n.py                         # 双语契约（需服务在跑）
```

> ⚠️ **沙箱 / 代理环境**：测本机 HTTP 必须绕代理，否则连接被劫持会误判成服务没起来。
> `curl` 加 `--noproxy '*'`；Python 用 `build_opener(ProxyHandler({}))`。

---

## 3. 代码地图

| 路径 | 职责 | 注意 |
|---|---|---|
| `public/` | 前端源码（`index.html`/`app.html`/`app.js`/`i18n.js`/`i18n-dict.js`/`i18n-dict-landing.js`/`style.css`/`landing.css`/`theme.css`）+ SEO | **改完必须跑内嵌脚本**（见 §4 约定 1）；无外部 CDN；`i18n-dict-landing.js` 是**派生产物**，漂移由 `tests/test_i18n_split.py` 9 条守卫把守 |
| `data/ap_macro.csv` | 世界银行真实种子（多维度 / 2019–2024 / 10 指标） | 脚本生成，已提交 |
| `data/nbs_cn.csv` | 中国国内明细（2024） | 带 BOM，读取处已处理 |
| `data/labels.csv` | **标识符标签包**（i18n 唯一事实来源） | 加词条不用改代码 |
| `data/qu_stats.db` | 运行时 SQLite | 已被 gitignore |
| `scripts/embed_pages.py` | 内嵌 `public/`→`src/pages.py`、`data/`→`src/seed_data.py` | 二进制按 base64 |
| `scripts/check_i18n.py` | 双语 API 契约检查 | 新增端点建议同步加进 CHECKS |
| `src/web.py` | Flask 应用：页面、REST API、错误页、冷启动播种、**ETag/gzip（本轮新增）** | 27 条路由，见 §6 |
| `src/api_docs.py` | `/docs` API 参考（服务端渲染双语，**端点清单是唯一事实来源**） | 改路由**必须**同步 `ENDPOINTS`，测试会校验 |
| `src/db.py` | SQLAlchemy Core over SQLite（指标宽表 + 知识库表） | **本轮：索引/ON CONFLICT/PRAGMA/幂等 init**；**连接池默认复用**（QueuePool，本地与 Vercel 一致），`QU_STAT_DB_POOL=null` 可退回 NullPool |
| `src/analyzer.py` | InfiniSynapse Server API 客户端（SSE） | **本轮：facts 注入 / 超时 / Skill 凭证链** |
| `src/infini_skill.py` | **本轮新增** · agent_infini Skill 规范化（凭证链/预检/审计/工作流） | 只读、缺 CLI 优雅降级 |
| `src/stats/sql_engine.py` | **本轮新增** · SQL 计算引擎（1 条 SQL 绑定全部变量） | 默认不启用，`QU_STAT_CUSTOM_ENGINE=sql` 切换 |
| `src/stats/indicators.py` | 各统计专业指标函数（维度锚定） | **本轮：`_val_unit()` 合并取值+取单位** |
| `src/stats/custom.py` | 自定义分析引擎（表达式沙箱） | 与 `sql_engine` 结果逐位一致 |
| `src/report.py` | 统计公报（结构化数据 + 文本渲染） | |
| `src/labels.py` | 标识符本地化（i18n 核心） | |
| `src/knowledge.py` | 双语知识库 + **中英概念映射语义召回**（轻量 RAG，零外部依赖） | 跨语言召回靠 `_CONCEPTS` 同义/别名表；要接向量检索，只换掉 `search_knowledge` 一个函数即可，调用方无感 |
| `src/kv_store.py` / `kv_sync.py` | 可选 KV 持久化与恢复（Vercel） | |
| `api/index.py` | Vercel Serverless 入口 | 见 §8 部署注意 |
| `src/pages.py` / `src/seed_data.py` | **生成物，勿手改**（但需提交） | CI 有漂移门禁 |

---

## 4. 六条不可违反的约定

1. **改完 `public/` 任何文件、或重新抓数后，必须跑 `scripts/embed_pages.py` 再重启服务。**
   Serverless 读不到 `public/`/`data/`，运行期靠内嵌副本。漏跑 → 你看到旧前端，会浪费大量时间排查"改了没生效"。
   文本按 UTF-8、二进制（`og-image.png`）按 **base64** 内嵌。

2. **数据标识符以中文规范键入库；对外本地化只发生在 `data/labels.csv` + `src/labels.py` 一处。**
   每标识符三层：`{field}` 本地化值 + `{field}_key` 中文规范键 + `{field}_slug` ASCII 键。
   前端**不再翻译数据**；字典里的数据词表是降级兜底，须与 `labels.csv` 逐字一致（`tests/test_labels.py` 把守）。

3. **未登记词条原样返回，绝不抛异常**（静默改写比报错更难排查）。

4. **API Key 一律走环境变量**，禁止写进被跟踪的 `config.yaml`。

5. **发布约定：每个独立变更批次单独一个 tag + release，不合并。**

6. **前端展示用的数据一律走公开端点，绝不依赖管理端点。**
   `/api/db` `/api/reseed` `/api/kv-status` `/api/collect` 部署后一律 403（线上真实踩过：静默功能缺失）。

> 本轮新增约定（建议遵守）：
> 7. **HTML 页面必须保持 `Cache-Control: no-store`**；ETag / 条件请求**仅对 JSON 接口**启用（`tests/test_web.py::test_html_is_no_store` 把守）。
> 8. **新增路由必须同步登记到 `src/api_docs.py::ENDPOINTS`**（`test_docs_covers_exactly_the_real_routes` 把守）。

---

## 5. 当前状态：已验证 vs 未验证

| 项 | 状态 |
|---|---|
| `pytest` 全量 **257 项** | ✅ 全部本地实测全绿（含冷启动 4 条 + 导入/upsert 契约 18 条 + 可观测性 14 条 + 知识库召回 34 条 + 连接池 9 条） |
| 入口冒烟 `/` `/app` `/api/stats` `/api/infini_skill` | ✅ 200 |
| SQL↔Python 自定义分析等价 | ✅ ALL MATCH（3 个真实分析） |
| 指标函数等值（Q1 改造后） | ✅ 数值一致 |
| ETag → 304；gzip -83% + 回环一致 | ✅ 实测 |
| 数据库查询计划 | ✅ 全部 `SEARCH … USING INDEX`，零全表扫描 |
| 本地服务 `/` `/app` `/docs` `/privacy` | ✅ 200 |
| 管理端点鉴权三态（本地放行 / 线上 403 / 配 token 校验） | ✅ 覆盖 |
| `basedpyright`（1.39.9，与 CI 同版本） | ✅ **0 errors / 0 warnings**（2026-09-25 实测） |
| SSE 中文编码回归（假云端，按 requests 真实语义伪造） | ✅ 2 条测试守住 |
| 前端静态文案字典覆盖（`data-i18n*` + `tr()` 字面量） | ✅ 2 条新测试守住；审计 95 处调用 0 缺失 |
| `/docs` 英文页代码块注释无中文 | ✅ 1 条新测试守住（29 个 `doc-pre` 块逐一审计，实测「修复前红」） |
| 冷启动导入卫生（`tests/test_cold_start.py`） | ✅ 4 条门禁；A/B 实测 `import src.web` CPU 390.6ms → 328.1ms |
| 批量写入与导入校验（`tests/test_import.py`） | ✅ 18 条门禁；729 行 upsert **206.6ms → 8.9ms**，`/api/reseed` 端到端约 10ms |
| `public/` ↔ `src/pages.py` 内嵌一致性（CI 漂移门禁） | ✅ 9/9 资产逐一比对一致；新增 `test_pages_module_is_in_sync_with_public_sources` 同步守卫（漏跑内嵌脚本直接红） |
| 慢查询 / 慢请求告警 + `/healthz`（`tests/test_obslog.py`） | ✅ 14 条门禁；真服务实测两类告警均按预期落盘 |
| 知识库跨语言语义召回（`tests/test_knowledge.py`） | ✅ 34 条门禁；35 条真实提问实测召回漏检 **17 → 0**、top-1 **18/35 → 31/35** |
| 连接池复用（`tests/test_db_pool.py`） | ✅ 9 条门禁；Vercel 形态从 NullPool 改为 QueuePool，端点 4–7 倍（`/api/insights` 4.77→0.99ms、`/api/overview` 3.79→0.80ms、`/api/stats` 3.60→0.98ms、`POST /api/knowledge` 4.49→0.60ms）；12 线程压测 0 错误 |
| `/api/infini_skill` 响应 | ✅ **56.32ms → 0.26ms**；消掉每请求 3 次 `shutil.which()` PATH 扫描（PATH 扫描结果进程内缓存 + `run_skill_cli()` 接受预解析 `cli`） |
| 前端载荷（gzip，HTML + 全部依赖资产） | ✅ 落地页首访 **22,417 → 18,753 B（−16.3%）**、重复访问 **9,281 → 6,518 B（−29.8%）**；看板首访 **54,012 → 54,578 B（+1.05%，诚实记录的回退）**；`/favicon.ico` 404 → 301 |
| 前端 JS 代码分割（`app.js` → core + 2 个按需分块） | ✅ 看板首访 **54,578 → 48,210 B（−11.7%）**、`app.js` 24,131 → 17,669 gz（−26.7%）；落地页首访零回归；分块 6,637 / 4,104 gz 仅用到才付；`refreshLang()` 的 `initCharts()` 补 `typeof` 守卫；`loadChunk()` 对无分块的 tab 直接兑现（**修掉拆分引入的指标面板回归**） |

---

## 6. 接口与配置速查

### 6.1 路由（35 条）
页面/静态：`/` `/app` `/docs` `/privacy` `/robots.txt` `/sitemap.xml` `/og-image.png` `/theme.css` `/style.css` `/landing.css` `/app.js` `/app.charts.js` `/app.palette.js` `/i18n.js` `/i18n-dict.js` `/i18n-dict-landing.js` `/favicon.svg` `/favicon.ico`（301 跳 `/favicon.svg`）
数据：`/api/overview` `/api/stats` `/api/indicators` `/api/export.csv` `/api/indicator_keys` `/api/custom` `/api/report`
AI：`/api/ask` · `/api/ai-cache` · **`/api/infini_skill`**
知识库：`/api/knowledge`
运维：`/healthz`
管理（403）：`/api/db` `/api/reseed` `/api/kv-status` `/api/collect`

### 6.2 环境变量（本轮新增以 ★ 标注）
| 变量 | 默认 | 作用 |
|---|---|---|
| `INFINISYNAPSE_API_KEY` | — | InfiniSynapse 密钥（优先于 config） |
| `AI_PROVIDER` | `infinisynapse` | `infinisynapse` / `openai_compat` |
| `OPENAI_API_KEY` / `OPENAI_BASE_URL` / `OPENAI_MODEL` | — | OpenAI 兼容 provider |
| ★ `QU_STAT_CUSTOM_ENGINE` | `python` | 自定义分析引擎：`python`/`sql` |
| ★ `INFINISYNAPSE_SQL_ENDPOINT` | 空 | 配置后 SQL 计算下推云端 |
| ★ `INFINISYNAPSE_CONSOLE` | 空 | 审计链接基地址 |
| ★ `INFINI_CONNECT_TIMEOUT` | `5` | AI 连接超时（秒） |
| ★ `INFINI_MAX_WAIT` | `45` | SSE 等待上限（秒） |
| ★ `AGENT_INFINI_CLI` | 空 | `agent_infini` 可执行文件路径（预检用） |
| `QU_STAT_DB_DIR` | — | Vercel 下指向 `/tmp/data` |
| `QU_STAT_ADMIN_TOKEN` | 空 | 管理端点令牌 |
| `QU_STAT_BASE_URL` | vercel.app | canonical / hreflang 基址 |
| `KV_REST_API_URL` / `KV_REST_API_TOKEN`（或 `UPSTASH_*`） | — | Vercel KV 持久化 |
| ★ `QU_STAT_SLOW_QUERY_MS` | `200` | 慢查询告警阈值（毫秒）；`<=0` 关闭 |
| ★ `QU_STAT_SLOW_REQUEST_MS` | `1000` | 慢请求告警阈值（毫秒）；`<=0` 关闭 |
| `QU_STAT_DB_POOL` | `queue` | 连接池类别；`null` 退回 NullPool（应急回退，免改代码） |

### 6.3 凭证链（批次 C）
`环境变量 > config.yaml > ~/.agent_infini/config.txt > ~/.agent_infini/config.json`

---

## 7. 已知风险与技术债

| 项 | 说明 | 建议 |
|---|---|---|
| 冷启动体积（**已优化，原判断已修正**） | `src/pages.py` 274KB 看着吓人，但 `compile()` 解析它只要 **2.6ms**，`import src.pages` 在 `import src.web`（约 330ms CPU）里只占 3.6ms；瓶颈是 `flask` + `sqlalchemy`。真正白花钱的是三个只在该分支才用到的顶层 import：`kv_store` 的 `requests`（连带 urllib3/email/http）、`custom.py` 与 `db.py` 的 `yaml` | 已改为函数内延迟导入，A/B 实测 `import src.web` CPU **−16%**；`tests/test_cold_start.py` 4 条门禁守住。**不要再做「拆分 pages.py 降冷启动」** |
| AI 解读缓存（已落地） | 同问同数据直接复用，不重复打云端；键覆盖提示词 + 事实文件 + 语言 + 模型身份，数据一变键就变 | `QU_STAT_AI_CACHE=0` 整体关闭；`/api/ai-cache` 看命中数 |
| SSE 中文乱码（**已修**） | 服务端 `text/event-stream` 不声明 `charset`，requests 的 `decode_unicode=True` 按 ISO-8859-1 解码，中文解读变乱码（已实测复现） | 已改为读原始字节后显式 UTF-8 解码，2 条回归测试守住；再接其他 SSE 源时保持同样处理 |
| 前端静态文案漏译（**已修 + 已加门禁**） | 英文界面曾因 3 个键不在字典而裸显中文：自定义分析的 2 个英文输入框占位符、命令面板的「示例问题」分组标签。根因是 `i18n.js` 的 `data-i18n`/`-html`/`-ph` 处理器只做精确查表，而 `check_i18n.py` 只审 API 响应，看不到 DOM 与前端拼装串 | 字典已补 3 条；`tests/test_labels.py` 新增 2 条覆盖测试，往后新增静态文案漏译会直接红 |
| `/docs` 英文页示例注释漏译（**已修 + 已加门禁**） | `/api/indicators` 的 `example` 是单字符串（含 `# 或按关键词搜索（中英均可）`），而 `_endpoint_html()` 只做 `str(ep["example"])`，英文页跟着显示中文注释。`check_i18n.py` 只审 API JSON，看不到服务端渲染的 HTML | 渲染器改为兼容「单串 / `(zh, en)` 二元组」两种形态（与 `body` 字段同构）；新增 `test_docs_english_page_has_no_chinese_comments`，注释行（`#` / `//`）出现中文即红 |
| 知识召回（**已大幅改善，云端半边未做**） | 原为纯中文二元组字面匹配，35 条真实提问 17 条召回不到 | 已落地中英概念映射 + 字段权重 + `source` 参与匹配，漏检 **17 → 0**、top-1 **18/35 → 31/35**；剩余 4 条 top-1 偏差均为「两条都相关、只是先后偏好」，已落在 top-2 内，不为调参而过拟合。云端 RAG 接入仍缺 key 与接口契约 |
| DB 层无查询缓存 | 有意后置（会破坏 `test_web.py` 隔离） | 若要做，先解耦该测试 |
| WAL 伴随文件 | 只读介质/网络盘上 WAL 可能不可用 | 已容错（逐条 try/except） |
| Vercel 无持久盘 | SQLite 落 `/tmp`，冷启动重建 | 依赖 KV 恢复（`kv_sync`） |

---

## 8. 部署注意（Vercel）

- 真部署入口是**根 `api/index.py`**（`from src.web import app` → `application = app`），`vercel.json` 路由到它。
- 部署**无构建步骤**：直接服务仓库里的 `src/pages.py` / `src/seed_data.py`（故约定 1 与 CI 漂移门禁很关键）。
- 部署方式（用户确认）：**推送 GitHub 触发自动部署**：`git push github master`。
  推送报 SSL 握手失败时用命令级覆盖绕过代理（勿改全局）：`git -c http.proxy= -c https.proxy= push github master`。
- 管理端点在线上**默认 403**；需要时配 `QU_STAT_ADMIN_TOKEN`。

---

## 9. 下一步建议（按优先级）

1. **P2 · K1（离线半边已落地，云端半边仍待）** `search_knowledge()` 已支持中英跨语言召回
   （概念映射 + 字段权重，35 条真实提问实测召回漏检 **17 → 0**、top-1 **18/35 → 31/35**，
   见 §5 / §7）；剩余的是接入 InfiniSynapse RAG **云端**检索，需要真实 key 与可文档化的
   检索接口契约，目前两项都缺。
2. **P3 · R2** 拿真实 InfiniSynapse key 跑一次云端解读，确认中文正常（编码修复已与服务端是否声明 `charset` 无关，这一步纯冒烟）。

> 已从清单划掉的五项：
> - **P3 · X1「结构化日志 + 慢查询告警 + `/healthz`」已完成**：慢查询告警用
>   `before_cursor_execute` / `after_cursor_execute` 配对计时，`handle_error` 里
>   同步出栈（失败语句不会让长连接上的时间栈无限增长）；`/healthz` 始终 200、
>   不播种不鉴权、字段集合恒定。详见 `CHANGELOG.md` 的 `[Unreleased]/Added`。
> - **P1 · S1「拆分 `src/pages.py` 降冷启动」实测否决**：`compile()` 解析这 274KB 仅 2.6ms，
> 占 `import src.web` 约 3.6ms，瓶颈在 flask / sqlalchemy。为 1% 的收益去动生成物、还要重新过
> embed 漂移门禁，并不划算。真正有效的冷启动优化（惰性导入 `requests` / `yaml`，CPU −16%）
> 已落地，详见 `CHANGELOG.md` 的 `[Unreleased]/Changed` 与 §7「冷启动体积」行。
> - **P2 · G1「导入校验 + 批量事务」已完成**：`upsert_indicators()` 改单条
>   `executemany`（729 行 **206.6ms → 8.9ms**，约 23 倍）；`load_file()` 与种子数据共用
>   `_coerce_row()` 校验，整份不可用时抛 `ValueError` 而非返回 0。详见
>   `CHANGELOG.md` 的 `[Unreleased]/Changed`。
> - **前端载荷优化已完成**：i18n 字典与运行时拆成三文件、落地页 622 行内联 `<style>` 外置为
>   `public/landing.css`、静态资源版本号改为按资产各自算 sha256、`/favicon.ico` 改 301。
>   落地页首访 gzip **22,417 → 18,753 B（−16.3%）**、重复访问 **−29.8%**；**看板首访 +1.05%**
>   （它本来就要全量字典，只多出两份文件的头部注释）——这项回退已在 CHANGELOG 与本节如实记录。
>   5 个回归场景逐一体过「该红就红」，详见 `CHANGELOG.md` 的 `[Unreleased]/Changed`。
> - **P2 · 前端按需加载（`app.js` 代码分割）已完成**：`public/app.js` 拆成 **core +
>   `app.charts.js` + `app.palette.js`**，首次用到时由 `loadChunk()` 动态注入 `<script>`
>   拉取——经典脚本共享全局词法环境，分块直接读写 core 里的 `const`，不需要打包器。
>   看板首访 **54,578 → 48,210 B（−11.7%）**，其中 `app.js` 24,131 → 17,669 gz（−26.7%）；
>   落地页首访**零回归**。两个分块各 **6,637 / 4,104 gz（懒）**，只有图表用户、打开命令
>   面板的用户才付。**代价一并记录**：打开图表 tab 后总字节略高于拆分前（多两个 HTTP 请求
>   以及分块各自的头部注释），这是按需加载的固有成本。行为由 `tests/frontend_behavior.mjs`
>   6 条哨兵把守，结构由 `tests/test_code_split.py` 6 条钉住。

---

## 10. 回退方法

本轮改动前的原始文件在 Agent 工作区 `backup_original/`（后缀 `.d`/`.p1` 对应批次）。
**回退原则**：逐文件覆盖还原即可；由于所有改动默认零行为变更，也可仅通过**不设置任何环境变量**保持原逻辑。

```bash
# 例：还原数据库层 / Web 层
copy backup_original\src__db.py.d    src\db.py
copy backup_original\src__web.py.p1  src\web.py
```

---

## 11. 交接清单（接手后建议逐项确认）

- [ ] `python -m pytest -q` → **283 passed**
- [ ] `.venv/Scripts/python.exe -m src.cli web --port 5000` → `/` `/app` `/docs` `/privacy` 均 200
- [ ] `python scripts/check_i18n.py` → 通过
- [ ] `curl -s http://127.0.0.1:5000/healthz` → 200 且 `status` 为 `ok`
- [ ] 读 `INFINISYNAPSE_INTEGRATION.md` + `AGENT_CHANGES.json`（本轮变更详情）
- [ ] 读本文件 §4 六条约定 + 新增约定 7/8
- [ ] 确认 `config.yaml` 内**无明文密钥**
- [ ] 了解发布流程（§8）与回退方法（§10）

---

## 12. 2026-09-25 追加：代码分割第二轮与三个回归修复

交接后又推进了一轮看板 JS 代码分割，把指标总表 / 自定义分析 / 统计公报三个 tab 也拆成
按需分块。拆分过程引入了 3 个真实回归并全部当场修复——**三者都是「结构测试抓不到、
只有行为测试才能抓」的那类**，现各有哨兵把守。

### 12.1 本轮改动

- 新分块 `public/app.indicators.js` / `public/app.custom.js` / `public/app.bulletin.js`，
  约定与图表 / 命令面板分块完全一致：core 不引用分块声明，入口只有 `openTab()` 的
  `.then` 回调与 `onLangRefresh()` 语言钩子。
- `src/web.py` 新增三条路由并登记 `_ASSET_FILES`；`scripts/embed_pages.py` 的 `FILES` 与
  重跑生成的 `src/pages.py` 同步；`src/api_docs.py` 前端资产 9 份改 12 份。
- `public/app.html`：`__CHUNK_URLS` 加三个条目；面板内按钮统一走
  `tabAction('custom', 'saveCustom')` 形式——传**名字**，不是裸函数名。

### 12.2 修掉的三个回归（均配红绿哨兵）

1. **首屏即炸**：`init()` 裸调已挪进 custom 分块的 `loadCustomList()`，首访就
   ReferenceError，整页初始化中断。改为不在首访预取下拉。
2. **切年份即炸**：`syncYear()` / `syncDimension()` / 采集回调裸调 `loadIndicators()`，
   用户没打开过指标 tab 时 ReferenceError，toast、分享链接一起失效。改为
   `refreshIndicatorsIfLoaded()`（`typeof` 守卫：打开过才刷新，没打开就跳过）。
3. **命令面板首访彻底打不开**：`tabAction()` 按引用传函数名，内联 onclick 点击瞬间求值，
   头部按钮是 palette 分块的唯一入口（快捷键绑定也在分块里），分块没加载就永远拉不下来。
   改为按名字在 `.then` 里解析；另修掉 palette 对 indicators / bulletin 分块的两处交叉引用。

### 12.3 实测数字（gzip，HTML 加全部依赖资产，LF 归一化）

- 看板首访 **48,055 → 46,746 B（−2.7%）**，`app.js` **17,814 → 16,384 B（−8.0%）**。
- 新分块 **2,345 / 2,860 / 1,859 B（懒）**；五个 tab 全打开过的重度用户总字节
  **+6,031 B**、多 3 个请求——按需加载的固有成本，如实记录。

### 12.4 测试

- `tests/frontend_behavior.mjs` 登记五个分块并新增 6 条行为哨兵；`tests/test_frontend.py`
  的 `SCENARIOS` 同步补登，另新增 2 条红绿哨兵；`tests/test_code_split.py` 的 `SPLIT_FILES`
  扩到 6 个文件，交叉引用检查推广到全部分块对，新增 `tabAction` 传名结构断言。
- **全量 260 项测试全绿**（本文件早期清单里的 239 已过期，以 260 为准）。

---

## 13. 2026-09-25 再追加：服务端取数三层优化

针对三个高频只读端点做了取数优化，每一层都有**同机配对实测**与新增测试把守。

### 13.1 本轮改动

- `src/db.py`：新增 `distinct_values(column)`（白名单 `year` / `category` / `dimension`，
  `SELECT DISTINCT` 走覆盖索引，`EXPLAIN` 已确认）与 `query_indicator_pairs(year, pairs)`
  （一条 `OR (专业=? AND 指标=?)` 取回多组合的全部维度行）。
- `src/stats/indicators.py`：`available_dimensions` / `available_years` / `all_categories`
  改调 `distinct_values()`；`_find()` 新增可选 `rows` 形参（预取行按回退顺序在内存挑首条）；
  `dimension_cards()` 用 `query_indicator_pairs()` 一条 SQL 预取上一年数据
  （「查过但无数据」与「未预取」用占位字典区分，否则退化逐组合点查）。
- `src/web.py`：KV 恢复路径两处「先查全表再 set 去重」改 `distinct_values()`；
  `/api/indicators` 与 `/api/export.csv` 的 `q` 过滤抽成 `_filter_by_query()`（语义不变）。
- `src/labels.py`：`normalize_lang` / `label` / `slug` / `localize_note` 四个纯函数加
  `@lru_cache`；`reload()` 同步清理全部缓存——漏清任何一个，改完 `labels.csv` 后接口
  都会继续返回上一版文案，这是正确性问题不是性能问题。

### 13.2 实测数字（同机配对：新旧两棵树交替、各 3 轮取中位数）

- `/api/stats`：**9.2ms → 1.6ms**。
- `/api/overview` 中国（SQL 条数）：**18.6–19.0ms（25 条）→ 2.8ms（6 条）**；
  亚太口径 **14.4–14.9ms（9 条）→ 2.7ms（5 条）**。
- `localize_indicators(en)`（729 行）：**5.8ms → 2.5ms**。
- `/api/indicators?q=` 端点：**15.1ms → 11.6ms**（不带 q）、
  **16.3ms → 12.4ms**（q=popul）。

如实记录：测量机器后台有远程桌面负载，绝对值是负载水位；前后两棵树同窗口测量，
差值可信。剩余开销去向：`query_indicators` 3.9ms、`q` 过滤 2.1ms、`jsonify` 1.4ms。

### 13.3 测试与门禁

- 新增 5 条：reload 必须失效全部纯函数缓存（换两张假词条表验证）、四个热点函数必须带
  `lru_cache`（结构哨兵，红绿已验证：摘掉装饰器即变红）、`q` 过滤三条行为测试
  （命中本地化英文值、命中中文规范键、空 q 与不带 q 逐行一致）。
- **全量 265 项测试全绿**；`scripts/check_i18n.py` 全部通过。

### 13.4 仍然开着的事项（本轮没做，接手的按需再推）

- `q` 过滤仍是整串 join + lower（约 2.1ms）：逐字段预过滤会改变跨字段语义，微基准
  也显示更慢，暂不动。
- `query_indicators` 3.9ms / `jsonify` 1.4ms 属固有开销；再往下要做响应缓存，
  需先解决「数据变更时的失效入口」，超出本轮范围。
- `public/og-image.png`（40KB truecolor，1200×630）可重压缩为调色板 / JPEG，
  它 base64 内嵌在 `src/pages.py` 约占 53KB。改动需重跑 `scripts/embed_pages.py`。
- **下一轮方向（本轮之后的新目标）**：学习开源项目的界面与交互范式，优化本项目的
  **美观与实用性**——优先从看板信息密度、空态 / 错误态、移动端适配三类实处入手。

---

## 14. 2026-09-26 追加：视觉精修第一批（学习开源项目 → 优化美观/实用性）

本轮目标从「性能」转向「美观与实用性」。做法是先下载三份真实开源资料当参照系
（Tailwind Preflight、shadcn/ui 的 globals.css、Pico.css），对照审计自身实现，
只采纳有出处的项，每项配结构哨兵（红绿已验证：删声明即变红）。全部改动落在
令牌与样式层，不动数据层、不动接口、不新增依赖。

### 14.1 本轮改动（4 项）

- **选中态主题化**：`theme.css` 增加 `::selection`，浅色 / 手动深色 / 跟随系统
  三个入口同步（theme.css 文件头「深色必须同步改」的纪律延伸）。
- **锚点跳转让出导航高度**：看板 `scroll-padding-top: calc(var(--header-h) + 8px)`、
  落地页 `72px`。修的是真实缺陷——sticky 52px / fixed 64px 的导航会把 `#main`、
  `#features` 等锚点落点整块盖住，键盘用户用 skip-link 像「什么都没发生」。
- **移动端基线**：`-webkit-text-size-adjust: 100%` + `-webkit-tap-highlight-color:
  transparent`（Tailwind Preflight 同款）。
- **数字等宽全覆盖**：`tabular-nums` 从 `.data-table .num` 扩展到 `.metric-value`、
  `.insight-val`、`.insight-rank` 与图表 SVG 文本（新增共享类 `.svg-num`，
  `app.charts.js` 的坐标轴刻度与排名条数值两处挂上）。

### 14.2 测试

- `tests/test_web.py` 新增 7 条结构哨兵：锚点让位、三入口 `::selection`、
  移动端两条基线、四个数字类必须 tabular-nums（参数化）。
- 全量 **272 项**测试全绿；`public/` 改动后已重跑 `scripts/embed_pages.py`。

### 14.3 仍然开着的事项

- 看板与落地页圆角取值不同（8/12 vs 10/16）是**有意约定**（`public/style.css`
  :root 处注释已说明），不要再当不一致去「修」。
- 建议下一批从这里挑（按性价比）：空态插图与文案分层、表格窄屏横向滚动的
  可感知提示（边缘渐隐）、图表悬浮读数在触屏上的等价交互、对比度抽检脚本化。
  （其中「边缘渐隐」已在 §15 落地，其余三项仍开着。）
- 截图走查路线备注：本机的 in-app 浏览器插件路径与运行时版本不匹配（沙箱按
  26.924.20706 找 `browser-service.mjs`，磁盘上只有 26.917.71314 的实体文件），
  本轮未做视觉截图，改以「代码审计 + 结构哨兵」闭环；后续若要做视觉回归，
  先把插件缓存版本对齐或改用自带 harness 的 svg 快照比对。

## 15. 2026-09-26 再追加：视觉精修第二批（宽表横向滚动 + 主题令牌第三入口）

### 15.1 本轮改动（4 项 + 1 个补丁）

- **宽表横向滚动的可感知提示**（§14.3 列出的第一个候选）：`.data-table-wrap` 用四层
  `background` 拼出「这里还能横向滑」的边缘阴影。两条 `radial-gradient` 是阴影本体，
  `background-attachment: scroll`，钉在容器视口上（不随内容滚，只在真有溢出时可见）；
  两条同色 `linear-gradient` 是遮挡层，`attachment: local`，随内容滚走、把已滚过处的
  阴影擦掉。**纯 CSS 判断溢出，零 JS**；Radix / shadcn 的 `ScrollArea` 是 JS 驱动，
  这里不需要那么重。
- **iOS 惯性滚动**：`.data-table-wrap` 补 `-webkit-overflow-scrolling: touch`。
- **七列表格的最小宽度**：`.data-table` 加 `min-width: 760px`。单选钮 / 指标 / 专业 /
  维度 / 数值 / 单位 / 来源七列，在窄屏原先被压成竖排碎片；现在整表横向滚动。
- **`.insight-chip b` 接上 `tabular-nums`**：第一批把等宽数字扩到三个位置，漏了洞察
  chip 里的数值，这条补上（顺手把该规则改成逐行声明，与相邻块风格一致）。
- **配套修正：`.data-table th` 不再铺底色（原 `--gray-25`）。** 纯 CSS 边缘阴影画在滚动
  容器的背景层上、位于表格**下方**，任何实底单元格都会把它挡住，左侧只剩半截阴影——
  这是该手法的前提（内容必须透背景）。表头原本的 `--gray-25` 与 `--surface` 在浅色下
  只差 1%，靠 2px 下边框 + 大写 + 字重区分已足够；已加哨兵钉住透明。
- **补丁：`--scroll-shadow` 补齐第三个主题入口。** 上一版只加在浅色与手动深色两处，
  既有门禁 `tests/test_web.py::test_theme_css_is_the_single_source_of_tokens` 立刻变红——
  它逐名比对手动深色 `:root[data-theme="dark"]` 与跟随系统
  `:root:not([data-theme="light"])` 两块令牌集合。**新增令牌必须三个入口一起改**，
  这正是该门禁存在的理由，补上后转绿。

### 15.2 测试

- `tests/test_web.py` 新增 5 条结构哨兵：边缘阴影四层与 `background-attachment` 分层、
  `.data-table` 最小宽度、chip 数值等宽、`--scroll-shadow` 三入口、表头必须透明。
  红绿已验证：逐条摘除对应声明各自变红，还原即绿（7 个变体全试过）。
- 全量 **277 项**测试全绿；改 `public/` 后已重跑 `scripts/embed_pages.py`。
  **坑**：`/style.css`、`/theme.css` 路由返回的是 `src/pages.py` 里的内嵌副本，
  不是磁盘上的 `public/`。所以「只改 `public/` 不跑 embed」时，跑的是 web 层门禁会
  读到旧内容——本轮就被这一点咬了一次（改完 `theme.css` 立刻红）。

### 15.3 仍然开着的事项

- 空态插图与文案分层；图表悬浮读数在触屏上的等价交互；对比度抽检脚本化。
- 截图走查仍走不通（见 §14.3 的 in-app 浏览器插件版本备注），继续用
  「代码审计 + 结构哨兵」闭环。

## 16. 2026-09-26 再追加：视觉精修第三批（emoji → 内联 SVG + 空态文案分层）

### 16.1 为什么换

- emoji 的字形与配色由操作系统决定（Windows / macOS / Android 三套设计语言），在空态圆底、
  特性卡这种大面积居中的位置差异最刺眼，且无法跟随主题令牌取色（深色模式下一排彩色
  emoji 尤其违和）。参照 heroicons / lucide / Bootstrap Icons 的做法换成内联线性 SVG。
- 空态原先只有一行标题（「暂无洞察」「加载失败」），用户看完不知道下一步能做什么；
  本批全部补上「下一步」说明，图标 / 标题 / 说明 / 出口四层分开。

### 16.2 落地方式

- `public/app.js` 新增 `ICONS` 白名单（24 个图标）+ `icon(name, cls)` 取图函数，
  未命中退回 `search`，永不渲染空白。`emptyState()` / `insightCard()` / `chartEmpty()`
  的入参从字符改成图标名；`themeApply()` 与命令面板从 `textContent` 改为 `innerHTML`
  （SVG 是标签，textContent 会把源码当文本显示——这是本批唯一踩到的坑）。
- `.icon-svg` 基类在 `style.css` 与 `landing.css` **各定义一份**（落地页不引 style.css），
  且必须是 `display: inline-block`：绝大多数图标与文字同行（洞察卡标题、AI 卡头、
  缓存徽标、按钮文案），`block` 会把行切断。
- 落地页 `index.html` 是自包含页面，特性 / 流程 / 架构卡的 SVG 直接写在 HTML 里，
  主题三态用内联脚本里一份最小的 `LANDING_ICONS`。
- 顺带修掉上一批引入的一处不一致：`.data-table th` 改透明（理由见 §15.1）。

### 16.3 测试

- 新增 3 条结构哨兵：前端不得再出现 emoji（扫 `public/**/*.js|html|css`）、
  `.icon-svg` 基类两个样式表都要有且为 `inline-block`、`icon()` 必须有白名单兜底。
- 另新增 1 条 i18n 门禁 `test_dashboard_tr_keys_exist_in_full_dict`：把 `public/app*.js` 里
  所有 `tr('字面量')` 的键与 `i18n-dict.js` 对照，缺一条就红——直接防「英文界面裸显中文」。
- 全量 **281 项**测试全绿；`tests/frontend_behavior.mjs` 63 项、`landing_theme_behavior.mjs`
  11 项全过；起真实服务冒烟：`/` 返回 16 个 `<svg>`、`/app` 10 个，全站 emoji 归零。

### 16.4 仍然开着的事项

- 图表悬浮读数在触屏上的等价交互、对比度抽检脚本化（见 §15.3）。
- 截图走查仍走不通（见 §14.3），继续用「代码审计 + 结构哨兵」闭环。

## 17. 2026-09-27 追加：WCAG 对比度抽检脚本化

### 17.1 做了什么

- 新增 `scripts/check_contrast.py`：把「前景色 / 背景色」配对从样式表**自动扫出来**
  （同一条规则里同时有 `color: var(--x)` 与 `background(-color): var(--y)`），
  三个主题入口各算一遍，`var()` 逐层解引用，半透明按 alpha 合成。
  阈值按字号分流：普通文本 4.5:1、大文本 3:1。
- 接成 pytest 门禁 `test_wcag_contrast_pairs_all_pass`，另配一条反向红绿
  `test_contrast_script_catches_a_regression`（把 `.empty-icon` 改回 `--gray-400`
  必须变红）——保证门禁本身不会静默失效。
- 首轮即抓到一个真实缺陷：空态图标描边 `--gray-400` 压在 `--gray-100` 圆底上
  只有 **2.31:1**（浅色），形状几乎看不出来；改为 `--gray-600` 后 6.87:1 / 8.51:1
  （浅 / 深），67 个配对全部达标。

### 17.2 已知边界（如实记录）

- 只扫**同一条规则内**的前景/背景配对。跨规则继承的组合扫不到
  （例如 `.data-table tr:hover td` 只设背景、文字色来自 `.data-table td`），
  这类要靠读代码补；脚本报告里已注明「同规则」口径。
- 半透明背景（`rgba(...,.85)` 的 `--nav-bg` 之类）跳过而非合成到页面底色——
  真实底色依赖滚动位置，脚本不猜。

### 17.3 仍然开着的事项

- 图表悬浮读数在触屏上的等价交互（见 §15.3）。
- 截图走查仍走不通（见 §14.3）。

---

## 附录 · 相关文档索引

| 文件 | 用途 |
|---|---|
| `HANDOFF.md` | 2026-09-17 旧版交接（更细的历史约定与验证矩阵） |
| `INFINISYNAPSE_INTEGRATION.md` | 本轮（A–E）变更完整说明（人读） |
| `AGENT_CHANGES.json` | 本轮变更结构化清单（机读） |
| `CHANGELOG.md` | 版本变更日志（`[Unreleased]` 含本轮） |
| `.codebuddy/memory/MEMORY.md` + `2026-09-23.md` | Agent 长期记忆 |
| `README.md` / `README.zh-CN.md` | 对外说明（中英） |
| `CONTRIBUTING.md` / `SECURITY.md` / `CITATION.cff` | 贡献 / 安全 / 引用 |
| `src/api_docs.py` | `/docs` 页面的唯一事实来源（端点清单） |